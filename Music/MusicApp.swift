import AppKit
import SwiftData
import SwiftUI

@main
struct MusicApp: App {

    var body: some Scene {
        WindowGroup {
            ContentView()
                .frame(minWidth: 1000, minHeight: 600)
        }
    }
}
